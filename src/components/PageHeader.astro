---
interface Props {
  title: string;
  subtitle?: string;
}
const { title, subtitle } = Astro.props;
---

<header class="mb-10">
  <h1 class="text-2xl font-bold tracking-tight md:text-3xl">{title}</h1>
  {subtitle && <p class="mt-2 max-w-2xl text-muted">{subtitle}</p>}
</header>
